%h1 Listing counties

%table
  %tr
    %th Name
    %th
    %th
    %th

  - @counties.each do |county|
    %tr
      %td= county.name
      %td= link_to 'Show', county
      %td= link_to 'Edit', edit_county_path(county)
      %td= link_to 'Destroy', county, :confirm => 'Are you sure?', :method => :delete

%br

= link_to 'New County', new_county_path
