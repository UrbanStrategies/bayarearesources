-if @page.errors.any?
  #error_explanation
    %h2= "#{pluralize(@page.errors.count, "error")} prohibited this county from being saved:"
    %ul
      - @page.errors.full_messages.each do |msg|
        %li= msg

.field
  = f.label :body
  = f.text_area :body, :class => 'tinymce'

.actions
  %br/
  = f.submit 'Save', :class => 'btn btn-success btn-large'
  &nbsp;&nbsp;
  = link_to 'Cancel', admin_pages_path